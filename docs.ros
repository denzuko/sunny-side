#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn
  (ros:ensure-asdf))

(let ((root (uiop:pathname-directory-pathname *load-pathname*)))
  (push root asdf:*central-registry*))

(asdf:load-system :sunny-side/docs :verbose nil)

(defun main (&rest argv)
  "Renders sunny-side's manual to standard output."
  (declare (ignorable argv))
  (sunny-side/docs:generate))
